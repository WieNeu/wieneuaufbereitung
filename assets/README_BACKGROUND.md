/* Mobile variant now available: CSS already loads /assets/background-small.webp for <=768px.

No change to content here; this commit only updates the README with confirmation and file size info. */

